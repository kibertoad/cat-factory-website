{
  "_comment": "Variables documented in docs/deploy/configuration.md tables that the code repo's canonical environment-variable list does not carry. Some are out of that list's scope by design (frontend build variables, UI-entered integration credentials); the rest are upstream gaps. scripts/sync-env-vars.mjs --verify fails when this set grows, so new drift is caught; the list should shrink over time as upstream catches up.",
  "missingFromCanonicalList": [
    "DOCUMENT_PLANNER",
    "DOCUMENT_SOURCES",
    "EMAIL_ENCRYPTION_KEY",
    "GITHUB_OAUTH_BASE",
    "GITHUB_PRIVILEGED_APP_ID",
    "GITHUB_PRIVILEGED_APP_PRIVATE_KEY",
    "GITHUB_WEBHOOK_SECRET",
    "GITLAB_CONNECTION_ID",
    "GITLAB_TOKEN",
    "GITLAB_WEBHOOK_SECRET",
    "INCIDENTIO_API_KEY",
    "LANGFUSE_BASE_URL",
    "LANGFUSE_ENABLED",
    "LANGFUSE_PUBLIC_KEY",
    "LANGFUSE_SECRET_KEY",
    "NUXT_PUBLIC_API_BASE",
    "PAGERDUTY_API_TOKEN",
    "PAGERDUTY_FROM_EMAIL",
    "PROMPT_LIBRARY_ENABLED",
    "PROVISIONING_DB",
    "SANDBOX_DB",
    "SLACK_CLIENT_ID",
    "SLACK_CLIENT_SECRET",
    "SLACK_REDIRECT_URL",
    "TELEMETRY_DB"
  ]
}
